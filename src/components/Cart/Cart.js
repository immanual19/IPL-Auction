import React from 'react';

const Cart = (props) => {
    return (
        <div>
        <div>
            <h2>Team Summary</h2>
            <h3>Player selected: {props.playersCart.length}</h3>
        </div>

        <div>
            
        </div>
        </div>
    );
};

export default Cart;